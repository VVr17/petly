import styled from 'styled-components';
import {
  typography,
  color,
  space,
  layout,
  shadow,
  border,
  flexbox,
  grid,
} from 'styled-system';

export const Box = styled('div')(
  typography,
  color,
  space,
  layout,
  shadow,
  border,
  flexbox,
  grid
);
